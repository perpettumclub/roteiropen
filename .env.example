# =====================================================
# ROTEIROPEN/HOOKY - Environment Variables
# =====================================================
# Copy this file to .env.local and fill in your values

# =====================================================
# FRONTEND (prefixo VITE_)
# =====================================================

# Supabase (REQUIRED for authentication and database)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NOTA: VITE_OPENAI_API_KEY foi REMOVIDO por segurança!
# A API key do OpenAI agora fica apenas no servidor.

# =====================================================
# BACKEND / SERVERLESS (sem prefixo VITE_)
# =====================================================

# OpenAI API Key (SEGURO - nunca exposto no frontend)
OPENAI_API_KEY=your_openai_api_key_here

# =====================================================
# VERCEL DEPLOYMENT CHECKLIST
# =====================================================
# 
# 1. Go to Vercel Dashboard → Project → Settings → Environment Variables
# 
# 2. Add these variables:
#    - VITE_SUPABASE_URL (frontend - exposto OK)
#    - VITE_SUPABASE_ANON_KEY (frontend - exposto OK, protegido por RLS)
#    - OPENAI_API_KEY (backend only - NUNCA exposto)
#
# 3. Redeploy your application
#
# SEGURANÇA (Método Israel Henrique):
# - Variáveis com VITE_ ficam expostas no frontend (OK para dados públicos)
# - Variáveis SEM VITE_ ficam apenas no servidor (para dados sensíveis)
