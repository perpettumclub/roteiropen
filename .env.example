# =====================================================
# ROTEIROPEN/HOOKY - Environment Variables
# =====================================================
# Copy this file to .env and fill in your values

# =====================================================
# FRONTEND (prefixo VITE_)
# =====================================================

# Supabase (REQUIRED for authentication and database)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI API Key (usado apenas em desenvolvimento local)
# Em produção, use OPENAI_API_KEY (sem VITE_) no Vercel
VITE_OPENAI_API_KEY=your_openai_api_key_here

# =====================================================
# VERCEL DEPLOYMENT (sem prefixo VITE_)
# =====================================================

# OpenAI API Key (SEGURO - nunca exposto no frontend em produção)
# Configure isso no Vercel Dashboard → Settings → Environment Variables
# OPENAI_API_KEY=your_openai_api_key_here

# =====================================================
# COMO FUNCIONA:
# =====================================================
# 
# LOCAL (npm run dev):
#   - Usa VITE_OPENAI_API_KEY diretamente
#   - OK porque é só seu computador
#
# PRODUÇÃO (Vercel):
#   - Usa OPENAI_API_KEY (sem VITE_)
#   - Serverless functions protegem a key
#   - Ninguém consegue ver a key no navegador
